import React from 'react';

const Footer = React.createClass({

  componentDidMount () {
    
  },

  render () {
    return (
      <footer>
        here is the footer
      </footer>
    );
  }
});

export default Footer;
